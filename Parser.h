#ifndef PARSER_H
#define PARSER_H
#include "MainCell.h"

class Parser/**Clase para subdividir la expresion que contiene una celda**/
{
    private:

    public:
        Parser();
        virtual ~Parser();
};

#endif // PARSER_H
